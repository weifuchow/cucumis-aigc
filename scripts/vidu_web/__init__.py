# vidu_web — Playwright-based browser automation for Vidu consumer membership.
# Implements the same MultimodalProvider interface as the API-based ViduProvider,
# by driving the Vidu web UI and intercepting its internal network calls.
