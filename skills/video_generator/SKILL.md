# video_generator

## Purpose

为需要动态表达的 scene 生成视频片段或镜头动画。

## Status

占位中。后续接入视频模型、镜头运动约束和结果校验。

## Planned Reads

- `projects/<project>/storyboard/storyboard.json`
- `projects/<project>/keyframes/keyframes.json`

## Planned Writes

- `projects/<project>/assets/video/`
- `projects/<project>/assets/manifest.json`
